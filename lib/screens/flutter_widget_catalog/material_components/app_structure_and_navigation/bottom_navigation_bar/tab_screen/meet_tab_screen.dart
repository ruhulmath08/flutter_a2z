import 'package:flutter/material.dart';

class MeetTabScreen extends StatelessWidget {
  const MeetTabScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Meet',
        style: TextStyle(fontSize: 70),
      ),
    );
  }
}
