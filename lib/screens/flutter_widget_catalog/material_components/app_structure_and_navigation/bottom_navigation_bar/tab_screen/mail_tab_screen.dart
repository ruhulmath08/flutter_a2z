import 'package:flutter/material.dart';

class MailTabScreen extends StatelessWidget {
  const MailTabScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Mail',
        style: TextStyle(fontSize: 70),
      ),
    );
  }
}
