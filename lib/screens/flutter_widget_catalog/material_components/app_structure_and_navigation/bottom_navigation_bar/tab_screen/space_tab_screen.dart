import 'package:flutter/material.dart';

class SpaceTabScreen extends StatelessWidget {
  const SpaceTabScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Space',
        style: TextStyle(fontSize: 70),
      ),
    );
  }
}
