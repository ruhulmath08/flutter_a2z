import 'package:flutter/material.dart';

class TreeViewTab extends StatelessWidget {
  const TreeViewTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Tree View Tab'),
    );
  }
}
