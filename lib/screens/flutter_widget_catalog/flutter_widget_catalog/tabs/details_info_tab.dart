import 'package:flutter/material.dart';

class DetailsInfoTab extends StatelessWidget {
  const DetailsInfoTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Details Info Tab'),
    );
  }
}
