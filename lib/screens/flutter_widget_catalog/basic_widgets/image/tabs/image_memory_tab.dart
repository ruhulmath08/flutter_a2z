import 'package:flutter/material.dart';

class ImageMemoryTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('ImageMemoryTab'),
    );
  }
}
