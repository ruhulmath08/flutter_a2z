import 'package:flutter/material.dart';

class ImageFileTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('ImageFileTab'),
    );
  }
}
