import 'package:flutter/material.dart';

class AdvancedTab extends StatelessWidget {
  const AdvancedTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Advanced'),
      ),
    );
  }
}
