import 'package:flutter/material.dart';

class ComplexTab extends StatelessWidget {
  const ComplexTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Complex'),
      ),
    );
  }
}
