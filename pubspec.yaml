name: flutter_a2z
description: A new Flutter application.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.0
  toast: ^0.1.5 #05-01-2020
  animated_floatactionbuttons: ^0.1.0  #17-01-2020
  syntax_highlighter: ^0.1.1  #17-01-2020
  url_launcher: ^5.7.10  #17-01-2020

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/
